spring:
  datasource:
    url: jdbc:h2:file:./data/careerlink-db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

careerlink:
  privacy:
    retention-days: ${CAREERLINK_PRIVACY_RETENTION_DAYS:30}
    masking-cron: ${CAREERLINK_PRIVACY_MASKING_CRON:0 0 3 * * *}
